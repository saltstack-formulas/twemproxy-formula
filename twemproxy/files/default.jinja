{%- from "twemproxy/map.jinja" import twemproxy with context -%}

# extra daemon options; see nutcracker --help

{%- for name, value in twemproxy.default | dictsort %}
{{ name }}="{{ value }}"
{%- endfor %}
